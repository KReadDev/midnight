package com.personal.manga.domain;

import android.os.Environment;

public class Constants {

  public static String location = Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOCUMENTS) + "/site/";
}
